# pii-proxy (Python)

## 0.1.0

- Initial public release.
- Synchronous `PiiProxyClient` with `httpx` backend.
- Dataclass result types for `anonymize`, `deanonymize`, `safe_call`.
- TypedDict `ExternalCall` for `safe_call`-Payload.
